# AgentGuard-X — pinned production dependencies
# Python 3.12 required
# IMPORTANT: Run pip-audit against this file before any deployment.

# ── Web framework (gateway + triage services) ─────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
starlette==0.41.3

# ── Async HTTP ────────────────────────────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.7
aiohttp==3.11.10
aiohttp-socks==0.10.1

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.7.0

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Redis (session store + rate limits + embedding cache) ─────────────────────
redis==5.2.1

# ── ChromaDB (vector store for Stage 4 RAG) ───────────────────────────────────
chromadb==0.6.3

# ── Sentence transformers (all-MiniLM-L6-v2, CPU) ────────────────────────────
sentence-transformers==3.3.1
torch==2.5.1               # CPU-only; pulled at seed time
torchvision==0.20.1

# ── Aho-Corasick (Stage 2 signature matching) ─────────────────────────────────
pyahocorasick==2.1.0

# ── OPA client ────────────────────────────────────────────────────────────────
opa-python-client==2.6.0

# ── PII scanning (Stage 4 / post-hook) ───────────────────────────────────────
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
# Presidio requires spacy model; pull at seed time (not committed)
spacy==3.8.3

# ── mitmproxy (TLS inspection proxy layer) ───────────────────────────────────
mitmproxy==11.0.2

# ── Observability (OpenTelemetry) ─────────────────────────────────────────────
opentelemetry-sdk==1.29.0
opentelemetry-api==1.29.0
opentelemetry-exporter-otlp-proto-grpc==1.29.0
opentelemetry-exporter-otlp-proto-http==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-redis==0.50b0
opentelemetry-instrumentation-httpx==0.50b0

# ── LangChain (gateway callback hooks) ───────────────────────────────────────
langchain==0.3.14
langchain-core==0.3.27
langchain-community==0.3.14

# ── Docker SDK (sandbox management) ──────────────────────────────────────────
docker==7.1.0

# ── Cryptography (mTLS, CA generation) ────────────────────────────────────────
cryptography==44.0.0
pyOpenSSL==24.3.0

# ── CLI / terminal ────────────────────────────────────────────────────────────
typer==0.15.1
rich==13.9.4
click==8.1.8

# ── Utilities ─────────────────────────────────────────────────────────────────
anyio==4.7.0
tenacity==9.0.0
packaging==24.2
cachetools==5.5.0
python-jose[cryptography]==3.3.0  # JWT for agent tokens
structlog==24.4.0
orjson==3.10.12
